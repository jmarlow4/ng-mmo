var app = angular.module('rl-app');

app.service('authService', function($location, $rootScope, sessionService, userService, $cookieStore) {
  this.login = function () {

  };
  this.register = function () {

  };
});